---
import Layout from '~/layouts/BaseLayout.astro';
import ImageUploader from '~/components/ImageUploader.tsx';
const user = Astro.locals.user; // Get user from middleware
---

<Layout title="优雅图床 - ImgBed">
    <!-- Hero Section with Gradient Background -->
    <div class="relative min-h-screen overflow-hidden">
        <!-- Background Elements -->
        <div class="absolute inset-0 bg-gradient-hero opacity-60"></div>
        <div class="absolute top-20 left-10 w-72 h-72 bg-primary/10 rounded-full blur-3xl animate-pulse"></div>
        <div class="absolute bottom-20 right-10 w-96 h-96 bg-primary/5 rounded-full blur-3xl floating"></div>

        <!-- Header -->
        <header
            class="relative z-10 container mx-auto px-4 md:px-8 py-6 flex justify-between items-center backdrop-blur-sm"
        >
            <div class="flex items-center gap-3">
                <div class="w-10 h-10 bg-gradient-to-br from-primary/20 to-primary/10 border border-primary/20 rounded-xl flex items-center justify-center shadow-lg backdrop-blur-sm">
                    <span class="material-symbols-outlined text-primary text-xl font-bold">cloud_upload</span>
                </div>
                <h1 class="text-2xl md:text-3xl font-bold bg-gradient-primary bg-clip-text text-transparent">
                    ImgBed
                </h1>
            </div>

            <div class="flex items-center gap-4">
                {user ? (
                    <a
                        href="/admin"
                        class="btn-enhanced btn-primary-enhanced px-6 py-3 rounded-full flex items-center gap-2 text-sm font-medium"
                    >
                        <span class="material-symbols-outlined text-base">dashboard</span>
                        <span class="hidden sm:inline">后台管理</span>
                    </a>
                ) : (
                    <a
                        href="/login"
                        class="btn-enhanced border-2 border-primary text-primary px-6 py-3 rounded-full hover:bg-primary hover:text-white transition-all duration-300 flex items-center gap-2 text-sm font-medium"
                    >
                        <span class="material-symbols-outlined text-base">login</span>
                        <span class="hidden sm:inline">登录后台</span>
                    </a>
                )}
            </div>
        </header>

        <!-- Main Content -->
        <main class="relative z-10 container mx-auto px-4 md:px-8 pt-8 pb-16">
            <!-- Hero Title -->
            <div class="text-center mb-16 animate-slide-in-up">
                <h2 class="text-fluid-2xl font-bold mb-6 text-text">
                    <span class="block mb-2">优雅、快速、安全的</span>
                    <span class="bg-gradient-primary bg-clip-text text-transparent">
                        云端图床服务
                    </span>
                </h2>
                <p class="text-lg md:text-xl text-text-secondary max-w-2xl mx-auto leading-relaxed">
                    基于 Cloudflare 全球网络，为您的图片提供极速访问体验。
                    支持拖拽上传、批量处理、实时编辑，让图片分享变得简单优雅。
                </p>
            </div>

            <!-- Features Grid -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-16 animate-fade-in" style="animation-delay: 0.2s;">
                <div class="card-enhanced p-6 text-center group">
                    <div class="w-16 h-16 bg-primary/10 rounded-2xl flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform duration-300">
                        <span class="material-symbols-outlined text-2xl text-primary">flash_on</span>
                    </div>
                    <h3 class="font-semibold text-lg mb-2 text-text">极速上传</h3>
                    <p class="text-text-secondary text-sm">
                        基于 Cloudflare R2 存储，全球 CDN 加速，上传和访问都飞快
                    </p>
                </div>

                <div class="card-enhanced p-6 text-center group">
                    <div class="w-16 h-16 bg-success/10 rounded-2xl flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform duration-300">
                        <span class="material-symbols-outlined text-2xl text-success">edit</span>
                    </div>
                    <h3 class="font-semibold text-lg mb-2 text-text">实时编辑</h3>
                    <p class="text-text-secondary text-sm">
                        内置图片编辑器，支持裁剪、旋转、缩放，编辑完成即刻上传
                    </p>
                </div>

                <div class="card-enhanced p-6 text-center group">
                    <div class="w-16 h-16 bg-info/10 rounded-2xl flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform duration-300">
                        <span class="material-symbols-outlined text-2xl text-info">security</span>
                    </div>
                    <h3 class="font-semibold text-lg mb-2 text-text">安全可靠</h3>
                    <p class="text-text-secondary text-sm">
                        防盗链保护、访问控制、数据加密，多重安全保障您的图片
                    </p>
                </div>
            </div>

            <!-- Upload Section -->
            <div class="animate-scale-in" style="animation-delay: 0.4s;">
                <ImageUploader client:only="preact" />
            </div>

            <!-- Upload Tips -->
            <div class="mt-12 card-enhanced p-8 animate-fade-in" style="animation-delay: 0.6s;">
                <h3 class="text-xl font-semibold mb-6 text-center text-text flex items-center justify-center gap-3">
                    <span class="material-symbols-outlined text-primary">tips_and_updates</span>
                    上传小贴士
                </h3>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                    <div class="text-center">
                        <div class="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center mx-auto mb-3">
                            <span class="material-symbols-outlined text-primary">drag_indicator</span>
                        </div>
                        <h4 class="font-medium mb-2 text-text">拖拽上传</h4>
                        <p class="text-sm text-text-secondary">
                            直接拖拽图片到上传区域
                        </p>
                    </div>

                    <div class="text-center">
                        <div class="w-12 h-12 bg-success/10 rounded-xl flex items-center justify-center mx-auto mb-3">
                            <span class="material-symbols-outlined text-success">content_paste</span>
                        </div>
                        <h4 class="font-medium mb-2 text-text">粘贴上传</h4>
                        <p class="text-sm text-text-secondary">
                            Ctrl+V 直接粘贴剪贴板图片
                        </p>
                    </div>

                    <div class="text-center">
                        <div class="w-12 h-12 bg-warning/10 rounded-xl flex items-center justify-center mx-auto mb-3">
                            <span class="material-symbols-outlined text-warning">library_add</span>
                        </div>
                        <h4 class="font-medium mb-2 text-text">批量处理</h4>
                        <p class="text-sm text-text-secondary">
                            同时选择多个文件批量上传
                        </p>
                    </div>

                    <div class="text-center">
                        <div class="w-12 h-12 bg-info/10 rounded-xl flex items-center justify-center mx-auto mb-3">
                            <span class="material-symbols-outlined text-info">folder</span>
                        </div>
                        <h4 class="font-medium mb-2 text-text">目录分类</h4>
                        <p class="text-sm text-text-secondary">
                            自定义目录，分类管理图片
                        </p>
                    </div>
                </div>
            </div>
        </main>
    </div>
</Layout>

<style>
    /* 确保动画延迟正确应用 */
    .animate-fade-in[style*="animation-delay"] {
        animation-fill-mode: both;
    }

    .animate-scale-in[style*="animation-delay"] {
        animation-fill-mode: both;
    }
</style>
